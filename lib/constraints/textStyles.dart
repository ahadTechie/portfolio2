import 'package:flutter/material.dart';

class TextStyles {
  // Base text style that only defines the font family
  static const TextStyle baseFont = TextStyle(
    fontFamily: 'Lufga',
    decoration: TextDecoration.none,
  );
}
